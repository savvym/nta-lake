/**
 * dataplat API TypeScript 类型。
 *
 * 本文件**禁止手写**——由 `make codegen` 从 FastAPI 的 OpenAPI JSON 生成。
 * 当前为 bootstrap-monorepo 占位，等 repo-api-mvp 变更引入 openapi-typescript 工具链后会被覆盖。
 */

export const PLACEHOLDER = true as const;
